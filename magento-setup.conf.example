# magento-setup.conf.example
# Copy to magento-setup.conf and fill in your values before running either script.
# Both setup-ubuntu24.sh and install-magento.sh read from this file.

# ============================================================
# SHARED SETTINGS
# ============================================================
DOMAIN_NAME="example.com"

# ============================================================
# SERVER SETUP (setup-ubuntu24.sh)
# All values are REQUIRED
# ============================================================
PHP_VERSION="8.4"                         # Required: 8.1, 8.2, 8.3, or 8.4
OPENSEARCH_VERSION="3.5.0"               # Required: e.g., 2.11, 2.15, 3.5.0
COMPOSER_VERSION="2"                     # Required: 2
MARIADB_VERSION="11.4"                   # Required: e.g., 10.11, 11.4, 11.8
MARIADB_ROOT_PASSWORD="change-me-strong-password"  # Required: strong root password
RESTRICTED_USER="magento"               # Required: Linux username for the Magento app user
SSH_PUBLIC_KEY="ssh-rsa AAAA...your-public-key"    # Required: full SSH public key for root login
PMA_PORT="61098"                         # Required: phpMyAdmin port (1–65535)
PMA_USERNAME="dbadmin"                  # Required: phpMyAdmin username
PMA_PASSWORD="change-me-strong-password"  # Required: phpMyAdmin password

# ============================================================
# MAGENTO DEPLOYMENT (install-magento.sh)
# All values are REQUIRED unless marked Optional
# ============================================================

# Git repository (SSH URL recommended, e.g. git@github.com:org/repo.git)
GIT_REPO_URL="git@github.com:your-org/your-magento-repo.git"

# Database — restricted user with access to DB_NAME only
DB_NAME="magento2"                       # Required: database name
DB_USER="magento_user"                   # Required: restricted DB username
DB_PASSWORD="change-me-strong-password"  # Required: strong DB password
DB_HOST="localhost"                      # Required: database host

# Database dump to import (Optional — leave empty to skip import)
# Supported: plain .sql file or gzip-compressed .sql.gz file
DB_DUMP_PATH="/path/to/database-dump.sql.gz"

# Media folder to import into pub/media/ (Optional — leave empty to skip)
# Supported: directory, .tar.gz, .tgz, .tar, .zip
MEDIA_PATH="/path/to/media"

# Admin credentials
ADMIN_FRONTNAME="admin"                  # Required: admin panel URL path (e.g. admin, weddingadmin)

# ============================================================
# GENERATED VALUES — populated automatically by the scripts
# Do NOT edit these manually
# ============================================================
# From setup-ubuntu24.sh:
# PMA_PATH="a1b2c3..."       # randomly-generated phpMyAdmin URL path
# MAGENTO_DIR="/var/www/example.com"

# From install-magento.sh:
# MAGENTO_DEPLOYED="yes"
# MAGENTO_DEPLOY_DATE="..."
