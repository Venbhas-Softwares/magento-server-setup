# magento-setup.conf.example
# Copy to magento-setup.conf and fill in your values before running either script.
# Both setup-ubuntu24.sh and install-magento.sh read from this file.

# ============================================================
# SHARED SETTINGS
# ============================================================
DOMAIN_NAME="example.com"

# ============================================================
# SERVER SETUP (setup-ubuntu24.sh)
# All values are REQUIRED
# ============================================================
PHP_VERSION="8.3"                        # Required: 8.1, 8.2, or 8.3
OPENSEARCH_VERSION="2.15"                # Required: e.g., 2.11, 2.13, 2.15
COMPOSER_VERSION="2"                     # Required: 1 or 2
MARIADB_VERSION="11.8"                   # Required: e.g., 10.11, 11.4, 11.8
MARIADB_ROOT_PASSWORD="change-me-strong-password"  # Required: Strong password
RESTRICTED_USER="magento"                # Required: Linux username for Magento
SSH_PUBLIC_KEY="ssh-rsa AAAA...your-public-key"    # Required: Full SSH public key
PMA_PORT="61098"                         # Required: phpMyAdmin port number
PMA_USERNAME="pma_admin"                 # Required: phpMyAdmin username
PMA_PASSWORD="change-me-strong-password"  # Required: Strong password

# ============================================================
# MAGENTO INSTALLATION (install-magento.sh)
# All values are REQUIRED
# ============================================================
DB_NAME="magento2"                       # Required: Database name
DB_USER="magento_user"                   # Required: Database user
DB_PASSWORD="change-me-strong-password"   # Required: Database password
DB_HOST="localhost"                      # Required: Database host
ADMIN_FIRSTNAME="Admin"                  # Required: Admin first name
ADMIN_LASTNAME="User"                    # Required: Admin last name
ADMIN_EMAIL="admin@example.com"          # Required: Admin email
ADMIN_USERNAME="admin"                   # Required: Admin username
ADMIN_PASSWORD="Admin123!"               # Required: Admin password (strong)
CURRENCY="USD"                           # Required: Store currency (e.g., USD, EUR, GBP)
TIMEZONE="America/Chicago"               # Required: Timezone (e.g., America/New_York, Europe/London)
LANGUAGE="en_US"                         # Required: Language locale (e.g., en_US, de_DE, fr_FR)
MAGENTO_PUBLIC_KEY="your-magento-marketplace-public-key"  # Required: From marketplace.magento.com
MAGENTO_PRIVATE_KEY="your-magento-marketplace-private-key"  # Required: From marketplace.magento.com
INSTALL_SSL="y"                          # Required: y or n (Install Let's Encrypt SSL)
SSL_EMAIL="ssl@example.com"              # Required if INSTALL_SSL=y: Email for SSL notifications

# ============================================================
# GENERATED VALUES - Auto-populated by scripts
# Do NOT edit these manually
# ============================================================
# These values are generated and appended during script execution

# From setup-ubuntu24.sh:
# PMA_PATH="a1b2c3d4e5f6g7h8..."        # phpMyAdmin URL path (randomly generated)
# MAGENTO_DIR="/var/www/example.com"    # Magento installation directory

# From install-magento.sh:
# MAGENTO_INSTALLED="yes"                # Installation status
# MAGENTO_BASE_URL="https://example.com/" # Store base URL
# MAGENTO_ADMIN_URL="https://example.com/admin" # Admin panel URL
# INSTALLATION_DATE="2026-02-17 14:30:00" # Installation completion timestamp
